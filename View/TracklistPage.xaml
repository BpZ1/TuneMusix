<Page x:Class="TuneMusix.View.TracklistPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:vm="clr-namespace:TuneMusix.ViewModel"
      xmlns:local="clr-namespace:TuneMusix.View"
      mc:Ignorable="d" 
      d:DesignHeight="623" d:DesignWidth="1195"
      Title="TracklistPage"
      DataContext="{DynamicResource TracklistViewModel}"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      TextElement.Foreground="{DynamicResource MaterialDesignBody}"
      TextElement.FontWeight="Regular"
      TextElement.FontSize="13"
      TextOptions.TextFormattingMode="Ideal"
      TextOptions.TextRenderingMode="Auto"
      Background="{DynamicResource MaterialDesignPaper}"
      FontFamily="{DynamicResource MaterialDesignFont}"
      
      >
    <Page.Resources>
        <vm:TracklistViewModel x:Key="TracklistViewModel"/>
    </Page.Resources>

    <Grid Background="#FFE5E5E5">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="5*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="5*"/>
        </Grid.RowDefinitions>


        <ListView Grid.Column="0" Grid.Row="0" x:Name="AllTrackList" ItemsSource="{Binding TrackList}"
                  SelectionChanged="TrackList_SelectionChanged"  Margin="10"  RenderTransformOrigin="0.503,0.478"
                  MouseDoubleClick="AllTrackList_MouseDoubleClick">
            <ListView.ContextMenu>
                <ContextMenu>
                    <MenuItem Command="{Binding PlayTrack}" Header="Play">
                        <!--<MenuItem.Icon>
                              <Image Source="Images/cut.png" />
                        </MenuItem.Icon> -->
                    </MenuItem>
                    <MenuItem Command="{Binding AddToPlaylist}" Header="Add to playlist">

                    </MenuItem>
                    <MenuItem Command="{Binding DeleteSelectedTracks}" Header="Delete">

                    </MenuItem>
                </ContextMenu>
            </ListView.ContextMenu>
            
            <ListView.View>
                <GridView AllowsColumnReorder="true" ColumnHeaderToolTip="Track Information">
                    <GridViewColumn DisplayMemberBinding= "{Binding Path=tTitle}"  Header="Title" Width="400"/>
                    <GridViewColumn DisplayMemberBinding="{Binding Path=tInterpret}" Header="Interpret" Width="200"/>
                    <GridViewColumn DisplayMemberBinding= "{Binding Path=tAlbum}" Header="Album" Width="100"/>
                    <GridViewColumn DisplayMemberBinding="{Binding Path=tYear}" Header="Year" Width="100"/>
                    <GridViewColumn DisplayMemberBinding="{Binding Path=tGenre}"  Header="Genre" Width="200"/>
                </GridView>

            </ListView.View>
        </ListView>
    </Grid>
</Page>

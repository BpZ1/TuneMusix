<UserControl x:Class="TuneMusix.View.Controls.AlbumItem"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
             xmlns:cmd="http://www.galasoft.ch/mvvmlight"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:local="clr-namespace:TuneMusix.View.Controls"
             mc:Ignorable="d">
  <Grid>
    <Grid.ColumnDefinitions>
      <!-- Cover art -->
            <ColumnDefinition Width="200" Name="ImageColumn"/>
      <!-- Info -->
            <ColumnDefinition Width="Auto"/>
    </Grid.ColumnDefinitions>
    <Grid.RowDefinitions>
            <RowDefinition Height="200"/>
    </Grid.RowDefinitions>
        <Grid x:Name="InfoGrid" Grid.Column="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="100"/>
                <ColumnDefinition Width="200"/>
                <ColumnDefinition Width="200"/>
                <ColumnDefinition Width="200"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="1*"/>
                <RowDefinition Height="1*"/>
                <RowDefinition Height="1*"/>
                <RowDefinition Height="1*"/>
            </Grid.RowDefinitions>
            <TextBlock Text="Name:" Grid.Column="0" Grid.Row="1"/>
            <TextBlock Text="{Binding Name}" Grid.Column="1" Grid.Row="1"/>
            <TextBlock Text="Tracks:" Grid.Column="0" Grid.Row="2"/>
            <TextBlock Text="{Binding TrackCount}" Grid.Column="1" Grid.Row="2"/>
            <TextBlock Text="Interpret:" Grid.Column="2" Grid.Row="1"/>
            <TextBlock Text="{Binding Interpret}" Grid.Column="3" Grid.Row="1"/>
            <TextBlock Text="Duration:" Grid.Column="2" Grid.Row="2"/>
            <TextBlock Text="{Binding Duration}" Grid.Column="3" Grid.Row="2"/>
        </Grid>
        <Image Source="{Binding CoverArt}" Grid.Column="0" Grid.Row="0" Width="200"/>
    </Grid>   
</UserControl>
